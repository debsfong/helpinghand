

export default (state = [], action = {type: ""}) => {
  Object.freeze(state);
  // switch (action.action.type) {
  //   case "RECEIVE_EVENTS":
  //     return state;
  //   default:
      return state;
  // }
};
